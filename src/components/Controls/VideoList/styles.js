import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  root: {
    overflowY: 'auto',
    height: '88%',
    borderRadius: '.25em',
  },
});

export default useStyles;
